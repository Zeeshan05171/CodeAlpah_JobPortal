/* ===========================================================
   JOBPORTAL — CONFIG
   Change ONLY this file if your Django backend runs on a
   different host/port. Every page loads this before api.js.
   =========================================================== */
window.__JOBPORTAL_API__ = "http://127.0.0.1:8000";
